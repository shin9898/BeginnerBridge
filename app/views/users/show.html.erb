<h1>マイページ</h1>

<div>
  <p><strong>ユーザー名:</strong> <%= @user.username %></p>
  <p><strong>メールアドレス:</strong> <%= @user.email %></p>
  <p><strong>所属:</strong> <%= @user.affiliation.presence || "未設定" %></p>
  <p><strong>サイトURL:</strong> <%= @user.site_url.presence || "未設定" %></p>
</div>

<% if user_signed_in? && current_user == @user %>
  <!-- 編集ボタンの表示：ログインしていて、かつ自分のマイページの場合のみ表示 -->
  <%= link_to "ユーザー情報を編集する", edit_user_path(@user), class: "btn btn-primary" %>
<% end %>

<% unless user_signed_in? %>
  <!-- 未ログインの場合でも表示されるメッセージ -->
  <p>ログインしていない場合は、ユーザー情報を編集することはできません。</p>
<% end %>
